# dependencies / reports
node_modules/
playwright-report/
test-results/
blob-report/
playwright/.cache/

# dotenv files (never commit real secrets)
.env
.env.local
.env.*.local
!.env.example

# OS / editor
.DS_Store

# local helper clones you created during checks
clean-check/
public-check/
